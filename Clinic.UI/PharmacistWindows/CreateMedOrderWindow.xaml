<Window x:Class="Clinic.UI.PharmacistWindows.CreateMedOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clinic.UI.PharmacistWindows"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Создание заявки" Height="350" Width="400">
    <Grid>
        <StackPanel>
            <ComboBox Width="150px" materialDesign:HintAssist.Hint="Препарат" Style="{StaticResource MaterialDesignFloatingHintComboBox}" x:Name="MedCb">
            </ComboBox>
            <TextBox Width="150px" materialDesign:HintAssist.Hint="Количество" Margin="0, 20" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
            <TextBox Width="150px" materialDesign:HintAssist.Hint="Примечание (необ.)" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
            <Button Content="Создать" Width="100px" Margin="0, 20"/>
        </StackPanel>
    </Grid>
</Window>
